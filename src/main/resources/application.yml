server:
  port: 8080

spring:
  application:
    name: Gateway_Application_Team9
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=1

        - id: admin-service
          uri: lb://admin-service
          predicates:
            - Path=/api/admin/**
          filters:
            - StripPrefix=1

        - id: recipe-service
          uri: lb://recipe-service
          predicates:
            - Path=/api/recipe/**
          filters:
            - StripPrefix=1

        - id: email-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/email/**
          filters:
            - StripPrefix=1

        - id: report-review-service
          uri: lb://report-review-service
          predicates:
            - Path=/api/report/**
            - Path=/api/review/**
          filters:
            - StripPrefix=1

logging:
  level:
    org.springframework.cloud.gateway: DEBUG  # 启用网关的调试日志
